name = "msinsight-nav-api"
main = "worker.js"
compatibility_date = "2024-01-01"

# 环境变量（在 Cloudflare Dashboard 中配置）
# SILICONFLOW_API_KEY = "your-api-key-here"

# Workers 配置
[vars]
ENVIRONMENT = "production"
