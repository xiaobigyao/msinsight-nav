# MindStudio Insight 环境变量配置

# 生产环境：SiliconFlow API Key（在 Cloudflare Pages 中配置）
SILICONFLOW_API_KEY=your-api-key-here

# 开发环境：直连 SiliconFlow API（可选，仅用于本地调试）
# 从 https://cloud.siliconflow.cn/account/ak 获取
NEXT_PUBLIC_API_KEY=your-api-key-here

# 注意：
# 1. 生产环境：SILICONFLOW_API_KEY 在 Cloudflare Pages 后台配置（服务端变量）
# 2. 开发环境：NEXT_PUBLIC_API_KEY 在 .env.local 中配置（客户端变量）
# 3. 使用 Next.js API Routes，无需配置 Worker URL
